interface:
  display_name: "Numo Narray"
  short_description: "Repo-specific patterns and workflows for numo-narray"
  default_prompt: "Use the numo-narray repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true